let array=process.argv,repStr="",subStr="",length=1;for(let r=0;r+length<=array[2].length;){subStr=array[2].substring(r,r+length);let t=3;for(;t<array.length&&array[t].includes(subStr);t++);t==array.length?(subStr.length>=repStr.length&&(repStr=subStr),length++):(r++,length=1)}console.log(repStr);